{
  "name": "magnetic-builds",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply magnetic-builds-production --local --config wrangler.toml --persist-to .wrangler/state",
    "db:migrate:remote": "wrangler d1 migrations apply magnetic-builds-production --remote --config wrangler.toml",
    "db:seed:local": "wrangler d1 execute magnetic-builds-production --local --config wrangler.toml --persist-to .wrangler/state --file seeds/piece-taxonomy.sql && wrangler d1 execute magnetic-builds-production --local --config wrangler.toml --persist-to .wrangler/state --file seeds/foundation.sql",
    "db:seed:remote": "wrangler d1 execute magnetic-builds-production --remote --config wrangler.toml --file seeds/piece-taxonomy.sql && wrangler d1 execute magnetic-builds-production --remote --config wrangler.toml --file seeds/foundation.sql",
    "db:verify:local": "wrangler d1 execute magnetic-builds-production --local --config wrangler.toml --persist-to .wrangler/state --file seeds/verify.sql",
    "db:verify:remote": "wrangler d1 execute magnetic-builds-production --remote --config wrangler.toml --file seeds/verify.sql"
  },
  "dependencies": {
    "@cloudflare/next-on-pages": "^1.13.16",
    "@drizzle-team/brocli": "^0.10.2",
    "@opennextjs/cloudflare": "^1.6.5",
    "drizzle-orm": "^0.44.5",
    "next": "15.5.7",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "@types/node": "^20.19.19",
    "@types/react": "^19.1.16",
    "@types/react-dom": "^19.1.9",
    "drizzle-kit": "^0.31.5",
    "eslint": "^9.39.1",
    "eslint-config-next": "15.5.7",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.45.0"
  }
}
